# v1.0.0 验收记录

当前四平台验证提交：`a5350e557ad744f0cf7163626601f08d049b6108`（尚未发布）。

## 2026-09-11 Windows 修复证据

- `a07adfd`：服务入口明确使用 UTF-8 读写标准流，修复 Windows cp1252 无法输出中文导致的启动退出。回归用真实服务进程和 cp1252 环境验证中文请求及中文错误响应。
- `06eb356`：打包验证等待运行按钮释放；结果显示后仍可能在保存学习记录，不再提前点击下一次运行。
- `3108ac2`：桌面 `prepackage`/`premake` 显式准备 Pyodide，修复 Windows ARM64 跳过 postinstall 后缺少模块的问题。清空生成资产后的准备步骤先复现失败，再验证修复。
- `a5350e5`：测试等待 SQLite 中实际出现完成记录，并在导航前等待运行锁释放，删除固定 500 毫秒延时。
- [2026-09-13 四平台验证运行](https://github.com/StewieCiao/python-agent/actions/runs/34756979880)：Windows x64/ARM64、macOS arm64/x64 的打包、原生 Python、页面运行检查全部通过。
- Windows x64 已验证打包 Python/SQLite、正确代码、真实异常、失败测试、4 秒超时恢复、重启持久化，以及系统安全存储到模型 HTTP 的请求链。模型 HTTP 使用测试服务器，不代表真实上游模型验收。
- 本地 Node 测试 274 项通过、Python 测试 64 项通过；lint（零警告）、桌面类型检查、macOS ARM64 打包及两项 packaged smoke 通过。

## 发布阻塞

远程 `v1.0.0` 标签已经指向旧提交 `30908ea6b175dee3cfd8c4eb03f6b6b343daeb52`，该版本不包含 Windows 修复，且目前没有对应 Release。按执行方案禁止覆盖旧标签，须确认改用新版本或明确授权移动该未发布标签。不得把 README 中预写的下载链接当作已交付资产。

## 已自动验证

- Node 测试：运行模块导入失败回归加入后，本地 275 项通过
- Python runtime 测试：发布前执行 `npm run test:python`
- lint、桌面 TypeScript 检查、主站/Pages/离线构建：发布前执行
- 四目标矩阵保留 macOS arm64/x64、Windows ARM64/x64
- Windows ARM64 的 `workerd` 安装故障由真实日志确认；该目标只跳过无关 web 安装脚本并显式重建 Electron

## 仍待实现或补充验收

- 已接入共享运行诊断与基础静态语法提示（锁定 `@lezer/python@1.1.19`，离线内联、无运行时网络请求）。279 项 Node 测试、lint、类型检查、离线构建及本机重新打包运行验证通过。静态提示仍不代表 Python 执行结果。
- 课程函数契约与上下文教学提示仍须补齐；离线 file:// 交互验收受当前浏览器 URL 安全策略阻止，不通过其他浏览器或代理绕过，不将静态构建检查冒充实际双击验收。
- 安装依赖时 npm 报告 50 项安全告警（含 2 critical）；尚需分析依赖路径及生产影响，未执行强制升级。
- 主站、桌面、离线版的输入法、撤销及三路线学习旅程仍须按执行方案补充实际交互证据。
- 最终候选代码变更后须重跑完整验证，不能直接复用上述 SHA 的结果。

## 需要在 Release 工作流核对

- 四个桌面产物均上传成功，文件名含版本、平台和架构
- `SHA256SUMS.txt` 与下载文件一致
- 匿名访问 Release 下载链接和 Pages 链接
- 在对应干净系统安装/启动；未签名警告如实记录

## 已知限制

- 本机无法替代 Windows 安装机验收；Windows 结果以 GitHub Actions 的真实产物和日志为准。
- 未配置模型时不进行真实上游模型请求；不会用模拟回答代替。
