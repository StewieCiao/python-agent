# v1.0.0 验收记录

当前四平台验证提交：`628651288af3b21b724150113c2e0bac8cdf6a64`（尚未正式发布）。

## 2026-09-18 Windows CI 退出清理修复

- 先前运行 `35167099512` / `35167881211` 在 Windows 完成业务检查后，强制终止 Electron 并立即删除 profile 时出现 `EBUSY`（Trust Tokens / Shared Dictionary）。这是测试收尾故障，不是 Python health 启动故障。
- `9a6e6eb` 的进程诊断没有证明修复：`35168125357` 的 Windows ARM64 反而在额外 `powershell.exe` / `Get-CimInstance` 查询上超时。已删除该诊断，不以它带来的延迟掩盖文件占用。
- `6286512`：成功路径改为与已有重启检查相同的 `Browser.close` 正常关闭握手，等待应用进程结束并确认退出码为 0，再删除临时目录。没有加入删除重试、忽略 EBUSY 或跳过平台。强制清理只保留在失败收尾路径。
- [四平台 packaged smoke 35351872954](https://github.com/StewieCiao/python-agent/actions/runs/35351872954) 全部通过。日志中 CDP 在关闭响应前断开是正常关闭的可观察现象；判定依据仍是进程退出和临时目录实际清理成功，不是忽略协议错误后直接报成功。
- Windows x64、Windows ARM64、macOS x64 均完成 Python 执行、异常、测试失败、超时恢复、学习状态重启恢复以及加密 Key 到测试 HTTP 服务的请求。macOS ARM64 CI 的系统安全存储不可用，验证的是明确拒绝且无部分配置残留，不声称该目标成功调用模型。
- 本地本批验证：279 项 Node 测试通过；修改脚本 ESLint 零警告；桌面类型检查、`git diff --check` 通过。本批未改应用业务代码或安装包；不将脚本验证冒充新安装包发布。
- 用户实体 Windows 的 `Python 服务请求 health 超时（10000ms）` 仍待目标机器诊断日志；本次 CI 修复不证明该故障已解决。

## 2026-09-11 Windows 修复证据

- `a07adfd`：服务入口明确使用 UTF-8 读写标准流，修复 Windows cp1252 无法输出中文导致的启动退出。回归用真实服务进程和 cp1252 环境验证中文请求及中文错误响应。
- `06eb356`：打包验证等待运行按钮释放；结果显示后仍可能在保存学习记录，不再提前点击下一次运行。
- `3108ac2`：桌面 `prepackage`/`premake` 显式准备 Pyodide，修复 Windows ARM64 跳过 postinstall 后缺少模块的问题。清空生成资产后的准备步骤先复现失败，再验证修复。
- `a5350e5`：测试等待 SQLite 中实际出现完成记录，并在导航前等待运行锁释放，删除固定 500 毫秒延时。
- [2026-09-13 四平台验证运行](https://github.com/StewieCiao/python-agent/actions/runs/34756979880)：Windows x64/ARM64、macOS arm64/x64 的打包、原生 Python、页面运行检查全部通过。
- Windows x64 已验证打包 Python/SQLite、正确代码、真实异常、失败测试、4 秒超时恢复、重启持久化，以及系统安全存储到模型 HTTP 的请求链。模型 HTTP 使用测试服务器，不代表真实上游模型验收。
- 本地 Node 测试 274 项通过、Python 测试 64 项通过；lint（零警告）、桌面类型检查、macOS ARM64 打包及两项 packaged smoke 通过。

## 发布阻塞

远程 `v1.0.0` 标签已经指向旧提交 `30908ea6b175dee3cfd8c4eb03f6b6b343daeb52`，该版本不包含 Windows 修复，且目前没有对应 Release。按执行方案禁止覆盖旧标签，须确认改用新版本或明确授权移动该未发布标签。不得把 README 中预写的下载链接当作已交付资产。

## 2026-09-18 本机完整验证

验证工作区提交：`a90407d`（与 `6286512` 之间仅使用文档和验收记录变更）。

- `npm ls postcss` 先以 `ELSPROBLEMS` 失败；锁文件指定 8.5.23，但已安装目录缺少 package.json。执行锁文件安装 `npm ci` 后恢复 8.5.23，未改依赖或锁文件，也未在业务代码增加兜底。
- `npm run test:all` 退出 0：Python 服务测试、279 项 Node 测试、ESLint 零警告、桌面类型检查、主站构建、离线生成、Pages 构建全部完成。生成内容未造成 Git 差异。
- `npm run demo:projects` 退出 0，项目演示使用确定性本地数据，不是外部模型验收。
- `npm run desktop:package` 退出 0，实际生成 macOS ARM64 应用；随后 `smoke:packaged-python` 和 `smoke:packaged-renderer` 均退出 0。
- 原生包报告 Python 3.13.15、SQLite 3.53.1（事务及 FTS5 成功）、LangChain 1.2.12、LangGraph 1.1.2、checkpoint-sqlite 2.0.6、pypdf 6.16.2。renderer 验证 Python 执行与恢复；当前本机安全存储不可用，验证了显式失败且无部分配置，不声称真实模型调用成功。
- 保留的警告：LangChain serializer 的 allowed_objects 默认值将变更、Forge/Vite 的 inlineDynamicImports 弃用、Web chunk 大于 500 kB 和 vinext 路由静态分类限制。依赖安装审计为 46 项风险（5 low、1 moderate、39 high、1 critical），没有执行强制自动修复。
- 发布标签冲突再次通过 `git ls-remote` / `gh release list` 确认，仍只有旧离线版 Release。正式下载交付没有完成。

## 已自动验证

- Node 测试：运行模块导入失败回归加入后，本地 275 项通过
- Python runtime 测试：发布前执行 `npm run test:python`
- lint、桌面 TypeScript 检查、主站/Pages/离线构建：发布前执行
- 四目标矩阵保留 macOS arm64/x64、Windows ARM64/x64
- Windows ARM64 的 `workerd` 安装故障由真实日志确认；该目标只跳过无关 web 安装脚本并显式重建 Electron

## 仍待实现或补充验收

- 已接入共享运行诊断与基础静态语法提示（锁定 `@lezer/python@1.1.19`，离线内联、无运行时网络请求）。279 项 Node 测试、lint、类型检查、离线构建及本机重新打包运行验证通过。静态提示仍不代表 Python 执行结果。
- 课程函数契约与上下文教学提示仍须补齐；离线 file:// 交互验收受当前浏览器 URL 安全策略阻止，不通过其他浏览器或代理绕过，不将静态构建检查冒充实际双击验收。
- 2026-09-14 定向升级 Next.js / eslint-config-next 到 16.3.5，并为 Pyodide 的 Node 依赖限定 ws 8.21.0。`npm audit --omit=dev` 为 0 项；依据包括 [Next.js Windows 服务端公告](https://github.com/vercel/next.js/security/advisories/GHSA-p293-qw3h-jr36)。未执行 `audit fix --force`。
- 全依赖审计仍为 46 项（开发依赖包含 1 critical 的 tar 6.2.1，路径为 Electron Forge → rebuild → node-gyp / cacache）。这是构建工具处理归档的风险，不将生产依赖清零表述为整个供应链无风险；审计建议降级 Forge，不直接采纳，后续需单独评估兼容升级。
- 上述升级后的 `test:all`、`demo:projects` 已通过：279 项 Node、64 项 Python、lint、桌面类型检查、主站、离线和 Pages 构建。四平台 CI 仍须针对新候选版本重跑。
- 主站、桌面、离线版的输入法、撤销及三路线学习旅程仍须按执行方案补充实际交互证据。
- 最终候选代码变更后须重跑完整验证，不能直接复用上述 SHA 的结果。

## 需要在 Release 工作流核对

- 四个桌面产物均上传成功，文件名含版本、平台和架构
- `SHA256SUMS.txt` 与下载文件一致
- 匿名访问 Release 下载链接和 Pages 链接
- 在对应干净系统安装/启动；未签名警告如实记录

## 已知限制

- 本机无法替代 Windows 安装机验收；Windows 结果以 GitHub Actions 的真实产物和日志为准。
- 未配置模型时不进行真实上游模型请求；不会用模拟回答代替。
